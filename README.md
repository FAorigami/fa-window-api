# FAWindow API

JavaScriptで簡単に「動かせる・重なる・状態が取れる」ウィンドウを表示するAPIライブラリ。

## 使い方
1. `FAWindow.js` と `FAWindow.css` を読み込む。
2. 以下のコードで表示：
```javascript
FAWindow.Show('ID', 100, 100, '#0078d4', '#fff', '<h1>Hello</h1>');
